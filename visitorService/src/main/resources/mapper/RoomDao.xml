<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sx.visitorService.dao.RoomDao">

    <resultMap type="com.sx.visitorService.entity.Room" id="RoomMap">
        <result property="roomId" column="room_id" jdbcType="INTEGER"/>
        <result property="capcity" column="capcity" jdbcType="INTEGER"/>
        <result property="state" column="state" jdbcType="INTEGER"/>
        <result property="personId" column="person_id" jdbcType="INTEGER"/>
        <result property="price" column="price" jdbcType="INTEGER"/>
        <result property="hotelId" column="hotel_id" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="RoomMap">
        select
          room_id, capcity, state, person_id, price, hotel_id
        from room
        where room_id = #{roomId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="RoomMap">
        select
          room_id, capcity, state, person_id, price, hotel_id
        from room
        <where>
            <if test="roomId != null">
                and room_id = #{roomId}
            </if>
            <if test="capcity != null">
                and capcity = #{capcity}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="personId != null">
                and person_id = #{personId}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="hotelId != null">
                and hotel_id = #{hotelId}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from room
        <where>
            <if test="roomId != null">
                and room_id = #{roomId}
            </if>
            <if test="capcity != null">
                and capcity = #{capcity}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="personId != null">
                and person_id = #{personId}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="hotelId != null">
                and hotel_id = #{hotelId}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="roomId" useGeneratedKeys="true">
        insert into room(capcity, state, person_id, price, hotel_id)
        values (#{capcity}, #{state}, #{personId}, #{price}, #{hotelId})
    </insert>

    <insert id="insertBatch" keyProperty="roomId" useGeneratedKeys="true">
        insert into room(capcity, state, person_id, price, hotel_id)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.capcity}, #{entity.state}, #{entity.personId}, #{entity.price}, #{entity.hotelId})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="roomId" useGeneratedKeys="true">
        insert into room(capcity, state, person_id, price, hotel_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.capcity}, #{entity.state}, #{entity.personId}, #{entity.price}, #{entity.hotelId})
        </foreach>
        on duplicate key update
        capcity = values(capcity),
        state = values(state),
        person_id = values(person_id),
        price = values(price),
        hotel_id = values(hotel_id)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update room
        <set>
            <if test="capcity != null">
                capcity = #{capcity},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
            <if test="personId != null">
                person_id = #{personId},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="hotelId != null">
                hotel_id = #{hotelId},
            </if>
        </set>
        where room_id = #{roomId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from room where room_id = #{roomId}
    </delete>

</mapper>

